import React from "react";

const Footer = () => (
  <footer style={{
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "60px",
    backgroundColor: "black",
    color: "#fff",
    fontSize: "14px",
    marginTop: "100%"
  }}>
    LightDuty(LD)
  </footer>
);

export default Footer;