import React from "react";

const Footer = () => (
  <footer style={{
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "60px",
    backgroundColor: "lightgreen",
    color: "#fff",
    fontSize: "14px",
  }}>
    LightDuty(LD)
  </footer>
);

export default Footer;